# Combined special-destinations gate

User authority on 13 September 2026 supersedes earlier Dubai/Makkah exclusions and spike recommendations. Both Palm Jumeirah and Makkah are authorized for a single selective integration attempt ending 14:36 SGT. 14:36–14:40 is deploy/public smoke only; app code freezes at 14:40.

Base: current v0.7.1, integration documentation HEAD991c2ee81099502bb37600c30df790438fbd159b. Source spike:4023b278d664f5d827d188413f1e7c71376b20dc. Historical spike handbacks remain unchanged and do not override current user approval. No wholesale merge.

Ownership: renderer worker adapts isolated destination assets/adapter and minimal engine hooks; orchestrator owns UI, disclosure, release, browser acceptance. Genesis, fast scales, prepared SG/NY caching, world shells and all existing commands must survive. YC actual Live implementation preempts destination debugging.

## Integrated gate observations — 14:36 SGT

Both destinations passed native Mac WebGL inspection at desktop and390×844 CSS-pixel phone width. Palm shows its trunk/fronds/crescent and mainland. Makkah shows a soft dispersed counter-clockwise field, not rigid concentric rings; its interpretive-flow/no-live-tracking disclosure remains visible after exploration and at phone width. No literal-person icons or tracking claims.

Native scale transitions: Palm regional primary entry1207ms, Makkah City→Street1193ms, phone Region→Makkah1205ms. Sampled renderer diagnostic near120fps on this Mac. These are session observations, not physical-phone or guaranteed end-user performance. Existing Singapore, New York and Challenger native round trips completed; destination switches retain their existing~6.8second cinematic transit. No Genesis or bridge-source changes.

TypeScript, production build, special-destination geography/lifecycle/counter-clockwise checks, Genesis and responsive-command checks pass. UI regression checks show destination buttons select the intended target. Captures reside in sibling demo-tooling/special-gate; historical spike documents remain unchanged.

YC actual branch arrived during the gate. The user extended the Live integration/deployment deadline to14:45. Integrated the minimal GPT-Live-1 WebRTC controller plus bounded five-target planner and hosted endpoints; deliberately omitted YC model library/database/rendering/data expansions. Native typing “Show me the vibe in New York.” starts movement on submission and settles in New York city, then explains. All10 transport checks,6 hosted-handler checks and navigation tests pass. Signed-out voice presents an explicit sign-in link. Public Site environment inspection returned no server variables at revision0, so actual Live audio requires OPENAI_API_KEY configuration and signed-in microphone verification; do not claim successful public speech.

Publication evidence follows after deployment. v0.7.1 remains the source and saved-publication fallback.
